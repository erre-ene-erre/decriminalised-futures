title: Site

columns:
  - width: 1/2
    sections:
      information:
        type: fields
        fields:
          metadescription:
            label: Meta Description
            type: textarea
            buttons: false
            help: 
              en: This description is what appears in Google search.
  - width: 1/2
    sections:
      generals:
        label: Information
        type: pages
        template: info
        image: false
      events:
        type: pages
        search: true
        template: event
        sortBy: datestart asc
        info: '{{ page.datestart.toDate("Y") }}'
        image: false
        flip: true
